# Antigravity working notes — internal only, not for the published repo
AGENTS.md
BLUEPRINT.md

# OS / editor
.DS_Store
Thumbs.db

# Node (in case tooling is added later)
node_modules/
npm-debug.log*

# Build output (if a bundler is introduced later)
dist/
*.map
